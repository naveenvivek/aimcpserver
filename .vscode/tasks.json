{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "Run MCP Weather Server",
			"type": "shell",
			"command": "uv",
			"args": [
				"run",
				"weather_server.py"
			],
			"group": "build",
			"isBackground": true,
			"problemMatcher": [],
			"options": {
				"env": {
					"PATH": "${env:HOME}/.local/bin:${env:PATH}"
				}
			}
		}
	]
}